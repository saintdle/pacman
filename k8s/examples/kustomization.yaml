apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - configmap.yaml
  - multi-role-deployment.yaml

patches:
  - path: deployment-env-patch.yaml
    target:
      group: apps
      version: v1
      kind: Deployment
      name: pacman
